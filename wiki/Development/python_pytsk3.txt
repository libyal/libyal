## Combining ${python_bindings_name} with pytsk3
```
import ${python_bindings_name}
import pytsk3

class ${development_prefix}_Img_Info(pytsk3.Img_Info):
  def __init__(self, ${development_main_object}):
    self.${development_main_object} = ${development_main_object}
    super(${development_prefix}_Img_Info, self).__init__()

  def close(self):
    self.${development_main_object}.close()

  def read(self, offset, size):
    self.${development_main_object}.seek(offset)
    return self.{development_main_object}.read(size)

  def get_size(self):
    return self.{development_main_object}.get_size()


${development_prefix}_${development_main_object} = ${python_bindings_name}.${development_main_object}()

${development_main_object_python_pre_open}
${development_prefix}_${development_main_object}.open("${development_main_object_filename}")

img_info = ${development_prefix}_Img_Info(${development_prefix}_${development_main_object})

fs_info = pytsk3.FS_Info(img_info, offset=63*512)
```

