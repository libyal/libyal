after_test:
- cmd: if [%TARGET%]==[mingw-w64-gcc-no-optimization] (
    copy C:\projects\codecov-bash\codecov C:\msys64\home\appveyor\${library_name}\codecov.sh &&
    C:\msys64\usr\bin\bash -e -l -c "cd ${library_name} && chmod a+x ./codecov.sh && ./codecov.sh -n $${TARGET}" )

