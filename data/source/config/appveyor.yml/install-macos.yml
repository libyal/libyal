- sh: if test $${BUILD_ENVIRONMENT} = "xcode"; then brew untap homebrew/homebrew-cask-versions && brew update -q; fi
- sh: if test $${BUILD_ENVIRONMENT} = "xcode"; then brew install -q ${brew_build_dependencies} || true; fi
