  black:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Check format of Python code
      uses: psf/black@stable
      with:
        options: "--check"
        src: "."
