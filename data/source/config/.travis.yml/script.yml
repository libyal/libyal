script:
- if test $${TARGET} != "coverity"; then export PATH=$$(echo $$PATH | tr ":" "\n" | sed '/\/opt\/python/d' | tr "\n" ":" | sed "s/::/:/g"); tests/build.sh $${CONFIGURE_OPTIONS} && tests/runtests.sh; fi
