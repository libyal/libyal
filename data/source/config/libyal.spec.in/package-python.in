%package -n ${library_name}-python3
Summary: Python 3 bindings for ${library_name}
Group: System Environment/Libraries
Requires: ${library_name} = %{version}-%{release} python3
BuildRequires: python3-devel python3-setuptools

%description -n ${library_name}-python3
Python 3 bindings for ${library_name}

