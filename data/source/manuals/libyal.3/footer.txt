.Sh DESCRIPTION
The
.Fn ${library_name}_get_version
function is used to retrieve the library version.
.Sh RETURN VALUES
Most of the functions return NULL or \-1 on error, dependent on the return type.
For the actual return values see "${library_name}.h".
.Sh ENVIRONMENT
None
.Sh FILES
None
${library_name} allows to be compiled with wide character support (wchar_t).

To compile ${library_name} with wide character support use:
.Ar ./configure --enable-wide-character-type=yes
 or define:
.Ar _UNICODE
 or
.Ar UNICODE
 during compilation.

.Ar ${library_name_upper_case}_WIDE_CHARACTER_TYPE
 in ${library_name}/features.h can be used to determine if ${library_name} was compiled with wide character support.
.Sh BUGS
Please report bugs of any kind on the project issue tracker: https://github.com/libyal/${library_name}/issues
.Sh AUTHOR
These man pages are generated from "${library_name}.h".
.Sh COPYRIGHT
Copyright (C) ${copyright}, ${authors}.

This is free software; see the source for copying conditions.
There is NO warranty; not even for MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.
.Sh SEE ALSO
the ${library_name}.h include file
