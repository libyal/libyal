## zlib
Download the latest zlib source distribution package including from the zlib project site: https://zlib.net/.
Extract the contents of the zlib source distribution package to the directory containing the ${project_name}-```<version>``` directory.
Normally the extracted zlib source directory will have a version number e.g. zlib-1.2.8, to remain version independent rename the directory to zlib.

